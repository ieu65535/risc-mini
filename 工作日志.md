# 工作日志

按 Git 提交记录已完成的工作。时间采用北京时间（UTC+8）；每条以提交哈希为准，不把未提交的开发过程记作完成。以后每次提交后（或下次继续开发时）核对 `git log`，为尚未记录的提交补一条，写明完成内容、验证结果及遗留事项。

## 2026-09-16 14:24:49 — `61053b0` `interrupt`

- 建立 EX/MEM/WB 第一版 `valid/kill/commit`，约束寄存器、CSR、Store 的副作用；验证中断前后指令的精确提交。
- 严格译码并加入非法指令（cause 2）、指令/Load/Store 地址未对齐（cause 0/4/6）异常；修正测试台同步指令存储器初始化顺序。
- 加入 Timer 请求保持和可读 `mip.MTIP`：MIE/MTIE 屏蔽期间不丢请求，同步异常优先，真正接收后清除 pending。
- Docker/Icarus 完整回归 `6 passed, 0 failed`；Vivado Simulator 2024.2 的 `tb_interrupt` 通过（3755 ns）。
- 遗留：CSR 地址/只读权限、性能计数器、真实定时器/CDC 与板级验证。

## 2026-09-15 22:22:10 — `fa944dd` `fix: prioritize trap redirect over pipeline stall`

- 调整 PC 更新优先级，让 Trap 重定向高于 load-use 停顿；补充两者同周期的定向测试。
- 修正 VS Code Icarus lint 的工作目录配置，减少找不到模块的误报；更新升级计划。

## 2026-09-15 21:04:14 — `77b2efd` `fix: gate timer interrupt with mie.MTIE`

- 在 `mstatus.MIE` 之外加入 `mie.MTIE` 门控，补充屏蔽/开放组合的定向测试。
- 更新 CSR、控制模块和阶段状态说明。

## 2026-09-15 20:37:44 — `8d84039` `finish simulation`

- 建立六项自检仿真基线及 `make regression-all` 入口，统一失败标记和超时判定。
- 修正 SoC/测试中的悬空输入、仿真文件表和 ROM 镜像生成约束；建立升级计划。
