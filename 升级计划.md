# CPU 升级计划

## 总体目标

- 完成赛题一基础与进阶要求。
- 优先提升正确性、主频和单位资源性能。
- 保持单发射顺序执行；暂不引入超标量和乱序执行。
- 按赛题目标形成明确的 IF、ID、EX、MEM、WB 五级流水；当前合并取指/译码前端仅是升级基线。
- 为 AI 加速器预留接口，不提前绑定具体网络结构。

## 当前进度（2026-09-17）

- 阶段 0：仿真基线与 Git 快照已完成；CoreMark、PDS 工程和上板验证暂缓。
- 阶段 1：当前 M-only、一拍同步存储接口的 CPU 行为基线已验收；已完成 Timer 请求保持、Trap 重定向优先级、EX/MEM/WB 第一版 `valid/kill/commit`、非法/未对齐/断点异常、CSR 地址/只读检查、`mtval`、`mtvec` Direct/Vectored、M-only CSR 字段约束、FENCE 及周期/退休指令/有效停顿计数器。官方架构一致性仍未验收。
- 当前已验证：Trap 边界与副作用精确；屏蔽期间 Timer 不丢失且同步异常优先；非法编码/CSR 访问产生 cause 2，`EBREAK` 产生 cause 3；指令、Load、Store 未对齐分别产生 cause 0、4、6，`mtval` 携带故障值。
- 当前计数器：64 位 `cycle/instret`（含机器态可写别名）和自定义只读 `stall_cycles`；Docker/Icarus 完整回归 11/11，Vivado Simulator 的计数器、中断、`mtvec`、CSR 字段、半字访存、FENCE 与 Trap 交叉定向测试通过。
- 上游指令单测：固定版本的 `riscv-tests` RV32UI 纯计算/控制流 30 项及访存 10 项，共 40/40 通过，失败码探针有效；不是 ACT4 认证或完整 ISA 一致性结果。
- 本地长序列：固定种子 xorshift32 运行 256 轮、12,403 仿真周期，交错执行分支、字节/半字/字写后读；16 个最终 RAM 字由宿主机独立计算并核对，故意错误的参考值被拒绝。
- 随机指令混合：4 个固定种子各生成 96 条运算指令，每组覆盖 10 类运算并插入 Store→Load→Branch 检查；宿主机 32 位参考值全部核对通过。
- Trap 交叉测试：4 个固定中断时序种子均通过真实 M-mode Handler 的 64 次循环、8 次 ECALL、18 次 Timer；覆盖 ECALL 冲突及 Load/Store EX 重合，RAM 计数独立核对；Icarus 与 Vivado Simulator 结果一致。这不是完全随机指令/异常联合生成。
- RV32I FENCE：当前无未完成访存的一拍顺序接口下作为可退休的无副作用指令；FENCE.TSO 与保留字段同样处理，FENCE.I 仍按未支持扩展产生非法指令。Cache/突发/DMA 接入前必须重做排序语义。
- ACT4 适配预检：当前 Docker 有 RISC-V GCC/Icarus，缺 Python/uv、Ruby/Bundler 与 Sail；还需本核 UDB 配置、`rvmodel_macros.h`、链接脚本及 ELF 批量运行适配，尚未运行 ACT4。
- 验收边界：`make phase1-behavior` 串行通过项目 11 项回归、上游 40 项及长序列/随机/Trap 交叉测试；一拍行为基线可冻结并进入阶段 2。ACT4 官方测试、可等待总线、PDS 综合/上板和 U/S 模式不在此结论内，需单列后续验收。

## 目标板资源与工程门槛

- 盘古100Pro+随板示例选择 `PG2L100H-6FBG676`；[Logos2 器件数据手册](https://www.pangomicro.com/en/uploads/13964872_1729131653.pdf)列出 66,600 个 LUT6、133,200 个 FF、155 个 36 Kbit DRM 和 **240 个 APM**。随板硬件手册的“740 APM”与器件手册及随板 PDS 示例的 `0 of 240` 报告冲突；规划按 240，不能把厂商视频示例的资源占用当作本 CPU 的占用。
- LUT6 用于 CPU/Cache/仲裁等通用逻辑，DRM 优先留给 Cache、FIFO、视频行缓冲与 AI 分块数据，APM 优先留给 INT8 并行乘加。具体映射、主频和余量必须以本项目 PDS 综合及布线后报告为准。
- DDR 控制器、PLL 等优先核对目标器件可用的 PDS IP 和随板例程；紫光官网列有支持 PG2L 的 [HMIC_S 存储接口 IP](https://www.pangomicro.com/en/resources/ip/Memory/)，实际配置及授权须在 PDS 中验证。旧 Xilinx XDC 不能当作此开发板的引脚/时钟约束。
- 阶段 0 的上板工作可继续暂缓，但进入性能结论前必须做一次最小 PDS 工程、DDR 例程、资源与时序摸底。CoreMark 应在 Cache/BTB 前后均测量，形成可比较基线。

## 中断、分支预测器与 Cache 的关系

- 共享基础：统一 PC 重定向、`valid/kill/commit` 和副作用门控。
- 控制优先级：统一仲裁 Trap、MRET、预测失败、Cache 等待与普通停顿。
- 分支预测：预测信息随指令流动；预测失败时重定向 PC、kill 年轻指令，禁止错误路径写回或访存。
- I-Cache：使用 `valid/ready` 取指；重定向后取消请求或用代次标识丢弃过期响应。
- D-Cache：仅允许已提交 Store 对外可见；Cache miss 暂停时保持流水线状态，避免请求重复。
- 地址属性：区分缓存区、非缓存区和 MMIO，明确访问顺序与一致性规则。
- 当前基础：已有 EX/MEM/WB 有效位、重定向优先级、Timer pending 和寄存器/CSR/Store 提交门控。
- 后续补充：IF/ID 握手、未完成请求标识、预测元数据寄存器，以及命中率、误预测和停顿计数器。

## 阶段 0：建立可靠基线

- 固化当前 RTL、软件和仿真结果。
- 完善自动判定成功/失败的回归测试。
- 跑通 CoreMark，记录主频、IPC、资源和得分基线。
- 确认目标开发板、FPGA 型号、存储器和 PDS 工程。

## 阶段 1：完善 CPU 正确性

- 引入流水级 `valid/kill/commit` 控制。
- 完成精确异常与中断机制。
- 支持非法指令、地址未对齐、ECALL、EBREAK 和定时器中断。
- 完善 CSR、MRET、暂停和流水线冲刷逻辑。
- 增加周期、退休指令和停顿等性能计数器。

### 本轮串行验收（2026-09-16）

| 顺序 | 验证门 | 结果 |
| --- | --- | --- |
| 1 | `make regression-all` | 11/11 项目定向测试通过，含新增 FENCE |
| 2 | `make isa-smoke` | 固定上游 RV32UI 40/40；256 轮长序列、4 种子×96 条随机运算及故意失败探针通过 |
| 3 | `make trap-mix` | 4 种子全部通过；每种子 8 次 ECALL、18 次 Timer，含冲突及 Load/Store 重合 |
| 4 | Vivado Simulator 2024.2 独立复核 | `tb_trap_mix` 4 种子、`tb_fence` 通过 |
| 5 | `make phase1-behavior` 最终串行重跑 | 前 3 门按顺序重新全部通过；当前一拍行为基线冻结 |

ACT4 官方套件需要独立运行环境、DUT/UDB 参数与结果接口适配；当前未执行，因此不能称“官方架构一致性通过”。阶段 0 的 PDS/开发板和 CoreMark 仍按约定暂缓；阶段 2 的 Cache/总线握手将改变访存时序，届时必须重跑以上基线并新增多周期请求验证。

## 特别篇章：阶段 1「完善 CPU 正确性」问题清单

本表以当前 `pipeline.sv` 主线和工作区为准，**包含尚未提交的改动**。`已仿真`表示项目定向行为测试覆盖了所述范围，并非通过官方 ISA 一致性、PDS 综合或上板验收；`部分`表示只有现有一拍存储器条件下的实现或测试；`待做（后续阶段）`表示阶段 1 必须先明确接口约束，但不要求现在造出 BTB、Cache 或 AI 加速器。

核心问题是：**哪条指令真实有效、何时可以产生不可撤销的副作用、出错后从哪里恢复**。这三件事若不先说清，后续性能模块越多，错误路径写入、重复请求和丢中断就越难排查。

| 大问题 | 小问题 / 验收点 | 解决的意义（知识点） | 对中断、预测、Cache、AI 的后续作用 | 当前状态 |
| --- | --- | --- | --- | --- |
| 指令语义 | 严格识别已支持的 RV32I 编码，未知 `opcode/funct3/funct7` 产生非法指令 | “取到一串比特”不等于“执行了一条合法指令”；不能让未知指令悄悄变成 NOP | 以后增加自定义 AI 指令时，必须显式扩展译码；未启用扩展的编码仍应可靠 Trap | 已仿真；上游部分单测已通过，尚无 ACT4 完整一致性结果 |
| 指令语义 | CSR 地址白名单、读写意图区分和只读 CSR 非法写入 | CSR 是架构状态，不存在的地址不能静默读 0；`CSRRS rs1=x0` 与 `CSRRW rs1=x0` 的写入语义不同 | 为以后新增预测统计、Cache 统计和加速器控制寄存器建立清晰的合法性边界 | 已仿真（当前 M 模式已实现的 CSR 子集） |
| 指令语义 | 区分 ECALL、EBREAK、非法指令的异常原因 | 三者都要转入处理程序，但软件处理方式不同；`EBREAK` 不是普通非法指令 | 为调试断点、系统调用和未来 AI 自定义指令异常分别提供可辨认原因 | 已仿真：cause 11、3、2 |
| 指令语义 | 补齐 FENCE 与 FENCE.I 的支持范围和验证 | FENCE 约束内存与设备可见顺序；FENCE.I 使先前写入对后续取指可见，两者不是同一件事 | Cache 引入后需要指令流同步；AI DMA/MMIO 启动前后需要明确内存与设备顺序 | 部分：一拍顺序接口的 FENCE/FENCE.TSO 已仿真；未实现 Zifencei 的 FENCE.I 明确 Trap；可等待存储的排序契约待阶段 2/4/6 |
| 指令语义 | 跑上游 ISA 单测、ACT4 与更多随机/交叉序列 | 项目自写 testbench 能证明指定场景，不能证明所有编码和边界都正确；上游单测也不等于认证 | 防止 BTB、Cache、AI 接入后把旧 CPU 缺陷误判为新模块问题 | 部分：固定上游 `riscv-tests` 40/40、4 种子×96 条随机运算、4 种子 Timer/ECALL 交错通过；仅 M-only、一拍存储，ACT4 未运行 |
| 流水线精确性 | EX/MEM/WB 携带 `valid`，冲刷产生 bubble 而非“伪指令” | `valid` 是这一级是否真的有指令；没有它就分不清空槽与 NOP | 预测失败、Trap、Cache 等待都可用同一语义管理在途指令 | 已仿真；IF/ID 尚无完整握手式有效位 |
| 流水线精确性 | 用 `kill/commit` 门控 GPR、CSR、Store 副作用 | 错误路径的计算可以被丢弃，写寄存器或写设备却通常不能撤销 | 保证误预测不修改软件状态；阻止被中断 Store 或 AI MMIO 命令“幽灵提交” | 已仿真（现有一拍存储接口） |
| 流水线精确性 | 验证 Trap 前老指令、当前故障指令、后续年轻指令的边界 | 精确异常要求老指令可完成，故障指令及被冲刷的年轻指令不能留下副作用 | 以后预测器的错误路径、D-Cache miss 和加速器 MMIO 写入都要遵守这条边界 | 已仿真（当前无未完成总线事务） |
| 流水线精确性 | 明确 PC 重定向与普通停顿的优先级 | Trap 发生时若先执行 `stall`，CSR 虽记下异常，PC 却可能永远没跳到 Handler | 当前统一处理 Trap/MRET/分支纠正；将来还须加入 Cache 等待与过期响应仲裁 | 部分：现有重定向高于 load-use 停顿；尚无 Cache 等待 |
| 流水线精确性 | IF/ID 增加 `valid/ready` 和取指请求身份 | 请求发出、响应返回、指令进入流水线是不同事件，慢存储器不能再假设当拍或下一拍成功 | I-Cache miss 后的旧响应必须能识别并丢弃；Trap/误预测不能执行旧路径指令 | 待做（阶段 2 接口，阶段 3/4 使用） |
| 流水线精确性 | 为预测信息建立随指令流动的寄存器 | EX 只能用“这条指令当时的预测”判断对错，不能拿当前前端的新预测比较 | BTB/BHT 的 `pred_taken/pred_target` 与 `valid/PC` 同步传递，误预测时精确回滚 | 待做（阶段 3）；当前只有静态预测 |
| 流水线精确性 | Store 在可等待接口下只发送一次，直到确认后才前进 | `mem_we` 拉高一个周期不等于外部存储器已经接受；暂停期间重复发请求会写两次 | D-Cache、突发总线和 AI MMIO 命令都需要请求保持、接受确认与一次性副作用 | 部分：当前 `commit_ex` 门控写使能；等待/重试协议未实现 |
| 异常现场 | `mepc/mcause/mtval` 分别记录故障 PC、原因、附加值 | 处理程序需要知道“哪条指令、什么错、哪个目标/地址或编码”；三者不能混用 | Cache/总线错误与 AI 外设错误以后可沿同一 Trap 现场机制定位 | 已仿真：现有非法、未对齐、ECALL、EBREAK、Timer 场景 |
| 异常现场 | 对 JAL、JALR、实际采用的 Branch 检查目标对齐 | RV32I 无压缩指令，目标须 4 字节对齐；未采用的 Branch 不应检查未使用目标 | 预测器可以猜目标，但只有真实执行目标决定异常；I-Cache 也不能把错误目标当有效取指 | 已仿真；含 taken/not-taken 与 JALR 交叉用例 |
| 异常现场 | 对 Load/Store 按访问宽度检查地址对齐 | 半字按 2 字节、字按 4 字节；故障访存不能改变寄存器或内存 | D-Cache 和 AI 共享存储器前先统一地址与写掩码语义 | 已仿真：LW/SW 与 LH/LHU/SH 的未对齐 Trap、`mtval`、无副作用；半字高低通道及符号扩展通过同步 RAM 测试 |
| 异常现场 | MRET 恢复 `mepc` 并恢复 MIE/MPIE | Trap 入口暂时关闭中断，返回时恢复原使能；否则会递归进入或永久屏蔽 | AI 完成中断也将复用返回通路；Cache 等待时不能让 MRET 丢失 | 已仿真（仅 M 模式、4 字节对齐返回）；U/S 模式和可等待访存未覆盖 |
| 异常现场 | 实现 `mtvec` BASE/MODE、Direct/Vectored 入口 | `mtvec` 低位是模式而非地址；Vectored 模式中中断与同步异常的入口计算不同 | 多中断源（Timer、AI 加速器）以后可分开入口；预测/Cache 重定向仍走同一 PC 通路 | 已仿真：Direct、Vectored ECALL/Timer；MODE 2/3 写入读回为 Direct；其他中断源未接入 |
| 异常现场 | 约束 `mstatus/mtvec/mepc/mie` 的可写字段和对齐 | CSR 不应允许软件写出本机无法兑现的状态；MRET/Trap 目标还需合法 | 避免预测器和 I-Cache 收到非法返回 PC，也为可靠中断嵌套打基础 | 已仿真：M-only 的 MIE/MPIE/MPP、MTIE、`mepc[1:0]=0`、`mtvec` MODE；未映射/不可执行地址仍待阶段 2 处理 |
| 异常现场 | 存储器返回错误时产生 access fault，并给出故障地址 | “地址对齐”不代表“目标存在且可访问”；总线错误不能被当作成功读取 0 | 外部 RAM、Cache refill、DMA 共享总线后需区分 Load/Store/取指失败 | 待做（阶段 2 接口）；当前总线无错误响应 |
| 中断可接收性 | 用 `mstatus.MIE` 与 `mie.MTIE` 双重门控 Timer | “中断到了”与“CPU 此刻允许接收”是两件事 | 将来 AI 中断也需要全局开关、分源使能和优先级定义 | 已仿真（Timer 一种来源） |
| 中断可接收性 | 屏蔽时锁存同步的一拍 Timer 请求，接收后清 pending | 短脉冲可能在关中断期间消失；pending 是事件记忆，不等于立即 Trap | AI 加速器完成脉冲也不能因 CPU 忙于 Cache miss 或关闭中断而丢失 | 已仿真（当前同步脉冲协议）；尚非标准电平型定时器 |
| 中断可接收性 | 同步异常与 Timer 同时发生时先处理同步异常 | 故障指令必须先得到确定处理，异步请求稍后再响应 | 未来预测失败、总线故障和多个外设 IRQ 同周期时要有可测试的统一仲裁 | 已仿真（ECALL/Timer 冲突）；多来源尚未定义 |
| 中断可接收性 | 在精确指令边界接收中断，不重复也不漏做 Store | 异步事件不能切断一个尚未完成且已经对外可见的操作 | Cache miss、写缓冲和 AI 命令启动后，需要等待/记录未完成事务再决定 Trap 时机 | 部分：固定一拍存储下已仿真；可等待事务未实现 |
| 中断可接收性 | 明确定时器/外设的电平、脉冲、清除和再次触发协议 | “清 pending”若不同时处理仍为高电平的源，会立刻再进中断 | AI 加速器完成状态通常需软件读状态并应答；不能只靠 CPU 清一个内部位 | 待做；当前仅约定同步单周期 `timer_int` |
| 中断可接收性 | 异步引脚或其他时钟域进入 CPU 前做 CDC | 异步信号可能在采样边沿亚稳，仿真通过也不能证明硬件安全 | 开发板定时器及 AI 加速器若不与 CPU 同钟，需要同步器或跨域握手 | 待做；当前 SoC 将 `timer_int` 固定为 0 |
| 中断可接收性 | 从单 Timer 扩到多来源的 ID、优先级和 pending | 多个请求同时挂起时，处理程序必须知道先服务谁、清谁 | 为 AI 加速器完成/错误中断预留并入方式，但现在不必固定加速器寄存器布局 | 待做（阶段 2/6）；目前只有 Timer |
| 中断软件契约 | 定义 Handler 保存寄存器、栈与可嵌套规则 | 硬件能 MRET 不代表 C 程序或 RTOS 能安全地被打断；上下文必须由软硬件共同维护 | FreeRTOS tick 与 AI 驱动中断都依赖稳定 Trap ABI | 部分：只有测试 Handler；无完整软件平台/嵌套验证 |
| 存储与 Cache 前置 | 明确当前 ROM/RAM 一拍同步返回的限制 | 现有流水线正确性建立在固定延迟上；换外存或 Cache miss 后不能沿用旧时序假设 | 阶段 2 先定义 IF/数据接口 `valid/ready`，再做 Cache 与突发 | 已明确限制；通用接口待做 |
| 存储与 Cache 前置 | 处理 PC 重定向后的过期取指/填充响应 | Trap 已换 PC，但旧 I-Cache 请求可能晚到；若直接接纳就执行错误路径 | 可用取消请求或代次/标签标记丢弃；BTB 误预测同样需要 | 待做（阶段 2/3/4） |
| 存储与 Cache 前置 | 区分普通 RAM、不可缓存 MMIO、ROM 的地址属性 | 设备寄存器读写常有副作用，不能像普通内存那样预取、合并或缓存 | AI 配置/状态寄存器必须走非缓存、有序通道；D-Cache 只缓存允许区域 | 部分：已有地址译码；Cache 属性与顺序规则未定 |
| 存储与 Cache 前置 | D-Cache miss、写策略和总线等待时保持请求与流水状态 | “暂停 PC”不足以处理多周期请求；还要避免重复 Store、丢 Load 返回值 | Trap/中断要知道旧请求是否已完成，AI DMA 还会与 CPU 争用总线 | 待做（阶段 2/4） |
| 存储与 Cache 前置 | 确定 DMA 缓冲区一致性方案 | AI 加速器直接读 RAM 时，CPU 脏数据可能还留在 D-Cache；DMA 写后 CPU 也可能读到旧缓存 | 可先选非缓存缓冲区，或定义软件 clean/invalidate；不能只加 DMA 接口就宣称可用 | 待做（阶段 4/6），现阶段只保留地址/顺序设计空间 |
| AI 扩展边界 | CPU 写“输入地址/长度/启动”MMIO 的可见顺序 | 先准备数据、后敲启动寄存器，硬件必须让加速器观察到这个顺序 | 将来应给驱动明确 FENCE/总线顺序要求，且启动写不得来自被 kill 的指令 | 部分：当前 Store 提交门控已有；MMIO 顺序协议未定 |
| AI 扩展边界 | 加速器中断携带完成/错误状态并可应答 | IRQ 只说明“有事”；处理程序还需读原因、清源、避免重复触发 | 复用现有 pending/Trap/MRET 基础，再扩充分源状态、优先级与清除协议 | 基础已仿真（Timer）；AI 中断未接入 |
| AI 扩展边界 | 若加入自定义指令，定义多周期执行与异常取消规则 | 自定义指令不应在被冲刷后仍启动加速器；长操作不能被当作一拍 ALU 指令 | 先定指令是否可中断、结果何时提交，再考虑译码和执行接口 | 待做（阶段 6）；目前未支持编码按非法指令处理 |
| 性能与验收 | `cycle/instret/stall` 计数与异常下的退休定义 | 周期、完成指令、有效暂停是三个不同量；Store/Branch 不写 GPR 仍可退休 | 给 BTB、Cache 和 AI 优化提供改动前后的共同基线 | 已仿真；64 位计数与现有停顿用例通过 |
| 性能与验收 | 分支误预测、Cache 命中/缺失、总线占用分类计数 | 总周期增加不能自动归因于某一个模块，必须按事件拆解 | 后续才能量化 BTB、2 路 Cache、突发和 AI DMA 是否真正有收益 | 待做（阶段 3/4/6） |
| 性能与验收 | 用上游 ISA/CSR 测试、长运行软件和随机压力检验边界 | 11/11 项目回归和 40 项上游单测仍覆盖有限；可复现的随机指令与中断时序能补相关性空白，但不能代替完全随机交叉或操作系统运行 | 防止阶段 2 以后“功能看似能跑，偶发数据错误” | 部分：上游 RV32UI 40/40；本地 256 轮/12,403 周期、4 种子×96 条随机运算及 4 种子 Timer/ECALL 交错通过；ACT4、完整 CSR、长运行软件待做 |
| 性能与验收 | ACT4 环境、DUT 配置和结果接口 | 认证套件必须按本核真实 ISA/特权参数选测试，并用参考模型给出预期；仅有交叉编译器和 Icarus 不够 | 固定 ISA 边界后再评估预测器/Cache 改动，避免将配置不匹配误判为 CPU 缺陷 | 已预检、未运行：现有 Docker 缺 Python/uv、Ruby/Bundler、Sail；UDB、结果宏与 ELF 适配待建，宜与日常轻量回归分开 |
| 性能与验收 | 建立 CoreMark、PDS 时序/资源和板级结果 | 行为仿真只证明给定时序模型下的逻辑；不证明能以目标频率在 FPGA 上运行 | 给未来 Cache、BTB、AI 加速器做性能/资源取舍，避免只看仿真周期数 | 待做；阶段 0 上板及 CoreMark 依约暂缓 |

**阶段 1 当前行为验收：**`make phase1-behavior` 串行通过 11 项项目回归、40 项固定上游 RV32UI、长序列/随机运算与 4 种子异常/中断交错；FENCE 与 Trap 交叉用例另经 Vivado Simulator 2024.2 独立复核。可冻结“现有 M-only、一拍同步 ROM/RAM”行为基线并进入阶段 2 的取指/数据握手设计。ACT4 与综合/板级不属于此次通过结论；Cache、动态预测和 AI 加速器仍分别属于后续阶段。

ACT4 官方入口：[框架与 DUT 配置要求](https://github.com/riscv/riscv-arch-test/blob/act4/README.md)、[独立 Docker 环境示例](https://github.com/riscv/riscv-arch-test/blob/act4/Dockerfile)。当前仅完成只读环境预检，不声称运行官方认证测试。

规范对照：[RISC-V 特权架构（Trap、CSR、mtvec）](https://docs.riscv.org/reference/isa/v20240411/_attachments/riscv-privileged.pdf)、[FENCE.I](https://docs.riscv.org/reference/isa/unpriv/zifencei.html)、[内存顺序与设备访问](https://docs.riscv.org/reference/isa/unpriv/rvwmo.html)。

## 阶段 2：升级存储与总线

- 将指令和数据访问改为握手式接口。
- 拆分当前合并前端，落实 IF、ID、EX、MEM、WB 五级，并将 `valid/kill/commit` 扩展到取指和译码；用可变等待、错误返回和过期响应测试守住精确执行。
- 设计统一的突发传输接口和仲裁机制。
- 划分缓存区、非缓存区和外设地址空间。
- 接入目标板上的片内 RAM 或外部存储器。

## 阶段 3：提升前端性能

- 实现 BTB 与 BHT 动态分支预测。
- 正确处理预测失败、跳转和异常冲刷。
- 统计分支命中率与误预测代价。

## 阶段 4：实现 Cache

- 实现 2 路组相联指令 Cache。
- 实现 2 路组相联数据 Cache。
- 支持突发填充、替换、写策略和非缓存访问。
- 统计命中率、缺失代价和总线占用。

## 阶段 5：CoreMark 与时序优化

- 在阶段 2 已形成五级流水的基础上，根据 PDS 布线后时序调整关键路径和流水级平衡。
- RV32M 仅在赛题范围与实测收益允许时单独评估；当前 RV32I 目标不预占 AI 所需 APM。
- 优化关键路径、停顿周期和突发传输效率。
- 对比 CoreMark/MHz、最高主频和 CoreMark/LUT。

## 阶段 6：接入 AI 加速器

- 采用 MMIO 配置、DMA 访存和中断通知。
- 预留加速器总线主设备接口与专用地址空间。
- 明确缓存一致性方案：非缓存缓冲区或软件刷新。
- 先实现最小 INT8 点积/卷积单元，再扩展完整应用。
- 根据最终模型决定是否增加自定义指令。

## 验收原则

- 每个阶段均包含定向测试和完整回归。
- 功能仿真通过后再进行综合、时序和资源评估。
- 新模块必须提供性能计数或可观测信号。
- 保留可运行版本，分阶段提交 Git。
