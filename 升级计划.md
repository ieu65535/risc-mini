# CPU 升级计划

## 总体目标

- 完成赛题一基础与进阶要求。
- 优先提升正确性、主频和单位资源性能。
- 保持单发射顺序执行；暂不引入超标量和乱序执行。
- 为 AI 加速器预留接口，不提前绑定具体网络结构。

## 当前进度（2026-09-16）

- 阶段 0：仿真基线与 Git 快照已完成；CoreMark、PDS 工程和上板验证暂缓。
- 阶段 1：进行中；已完成 Timer 门控与 `mip.MTIP` 请求保持、Trap 重定向优先级、EX/MEM/WB 的第一版 `valid/kill/commit`、非法指令及地址未对齐异常。
- 当前已验证：Trap 边界与副作用精确；屏蔽期间 Timer 不丢失且同步异常优先；非法编码产生 cause 2；指令、Load、Store 未对齐分别产生 cause 0、4、6。
- 下一项：补充 CSR 地址/权限合法性，再加入周期、退休指令和停顿计数器。

## 中断、分支预测器与 Cache 的关系

- 共享基础：统一 PC 重定向、`valid/kill/commit` 和副作用门控。
- 控制优先级：统一仲裁 Trap、MRET、预测失败、Cache 等待与普通停顿。
- 分支预测：预测信息随指令流动；预测失败时重定向 PC、kill 年轻指令，禁止错误路径写回或访存。
- I-Cache：使用 `valid/ready` 取指；重定向后取消请求或用代次标识丢弃过期响应。
- D-Cache：仅允许已提交 Store 对外可见；Cache miss 暂停时保持流水线状态，避免请求重复。
- 地址属性：区分缓存区、非缓存区和 MMIO，明确访问顺序与一致性规则。
- 当前基础：已有 EX/MEM/WB 有效位、重定向优先级、Timer pending 和寄存器/CSR/Store 提交门控。
- 后续补充：IF/ID 握手、未完成请求标识、预测元数据寄存器，以及命中率、误预测和停顿计数器。

## 阶段 0：建立可靠基线

- 固化当前 RTL、软件和仿真结果。
- 完善自动判定成功/失败的回归测试。
- 跑通 CoreMark，记录主频、IPC、资源和得分基线。
- 确认目标开发板、FPGA 型号、存储器和 PDS 工程。

## 阶段 1：完善 CPU 正确性

- 引入流水级 `valid/kill/commit` 控制。
- 完成精确异常与中断机制。
- 支持非法指令、地址未对齐、ECALL 和定时器中断。
- 完善 CSR、MRET、暂停和流水线冲刷逻辑。
- 增加周期、退休指令和停顿等性能计数器。

## 阶段 2：升级存储与总线

- 将指令和数据访问改为握手式接口。
- 设计统一的突发传输接口和仲裁机制。
- 划分缓存区、非缓存区和外设地址空间。
- 接入目标板上的片内 RAM 或外部存储器。

## 阶段 3：提升前端性能

- 实现 BTB 与 BHT 动态分支预测。
- 正确处理预测失败、跳转和异常冲刷。
- 统计分支命中率与误预测代价。

## 阶段 4：实现 Cache

- 实现 2 路组相联指令 Cache。
- 实现 2 路组相联数据 Cache。
- 支持突发填充、替换、写策略和非缓存访问。
- 统计命中率、缺失代价和总线占用。

## 阶段 5：CoreMark 与时序优化

- 根据综合时序决定是否调整为更清晰的五级流水线。
- 评估使用 DSP 实现 M 扩展的收益。
- 优化关键路径、停顿周期和突发传输效率。
- 对比 CoreMark/MHz、最高主频和 CoreMark/LUT。

## 阶段 6：接入 AI 加速器

- 采用 MMIO 配置、DMA 访存和中断通知。
- 预留加速器总线主设备接口与专用地址空间。
- 明确缓存一致性方案：非缓存缓冲区或软件刷新。
- 先实现最小 INT8 点积/卷积单元，再扩展完整应用。
- 根据最终模型决定是否增加自定义指令。

## 验收原则

- 每个阶段均包含定向测试和完整回归。
- 功能仿真通过后再进行综合、时序和资源评估。
- 新模块必须提供性能计数或可观测信号。
- 保留可运行版本，分阶段提交 Git。
